# ── Core ──────────────────────────────────────────────────────────────────────
pydantic>=2.0
openpyxl>=3.1.0

# ── Server ─────────────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
httpx>=0.27.0        # Used for Claude (Anthropic) API calls

# ── Git integration ────────────────────────────────────────────────────────────
gitpython>=3.1.40    # needed for /extract/from-git endpoint

# ── Universal format support ───────────────────────────────────────────────────
# stdlib only (zipfile, json, re, ast, xml) — no extra packages needed

# ── Dev / testing (optional) ──────────────────────────────────────────────────
# pytest>=8.0
# pytest-asyncio>=0.23

# NOTE: Claude API key setup
# Get key at: https://console.anthropic.com  (starts with sk-ant-)
# Set env var:  export CLAUDE_API_KEY=sk-ant-...
# OR paste it into the UI sidebar at runtime via the Claude pill in the topbar.
